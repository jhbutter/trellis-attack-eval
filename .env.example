# Optional: enable cloud submission storage.
# Without these variables, the app still works online, but each evaluator's records stay in their browser localStorage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional for GitHub Pages. Example: /trellis-attack-eval/
VITE_BASE_PATH=/
